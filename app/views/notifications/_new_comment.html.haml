%div
  = link_to notification.actor.email, main_app.user_path(notification.actor)
  has commented in
  \#{link_to notification.second_target.content, main_app.article_path(notification.second_target)}
%div
  = notification.target.content
